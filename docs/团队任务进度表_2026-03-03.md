# ContainerGuard AI — 团队任务进度表

> 更新日期: 2026-03-03 | 阶段二截止: 3.8 | 阶段三截止: 3.22 | 阶段四截止: 4.5

---

## 一、整体进度概览

| 成员 | 角色 | 阶段一 | 阶段二 | 阶段三 | 阶段四 | 总体 |
|:---|:---|:---|:---|:---|:---|:---|
| **金韩溢** | 组长 / Agent 架构 | 7/7 ✅ | 5/5 ⚠️ | 1/2 🟡 | 0/1 | **13/15** |
| **卢周全** | 知识图谱 / 检索 | 0/3 ❌ | 0/4 ❌ | 0/1 | 0/1 | **0/9** |
| **邓一凡** | 测试数据 / QA | — | 3/4 🟡 | 0/1 | 0/1 | **3/6** |

> ⚠️ 金韩溢阶段二"完成"打引号：5 个 Agent 框架搭好了，但 **Agent ↔ Skill 没有真正打通**，属于核心质量问题。

---

## 二、金韩溢 — 已完成 / 待修复 / 待开发

### ✅ 已完成（质量过关）

| # | 任务 | 所属阶段 | 说明 |
|:---|:---|:---|:---|
| 1 | 环境搭建 + Blueprint 跑通 | 一 | Windows + Mac 均验证通过 |
| 2 | BaseSkill 抽象基类 | 一 | 设计规范，含 `run()`/`validate()` |
| 3 | SkillRegistry 注册机制 | 一 | 装饰器自动注册，可扩展 |
| 4 | IntelSkill 情报检索 | 一 | NVD/GHSA/RedHat/Ubuntu 多源 |
| 5 | ConfigSkill SBOM 解析 | 一 | 支持 file/http/manual，修复 BOM 头 |
| 6 | RemoteCodeSkill 远程代码 | 一 | GitHub API 搜索，绕过本地 clone |
| 7 | Skills 单元测试 13/13 | 一 | 全部通过 |
| 8 | Supervisor 状态机 | 二 | LangGraph StateGraph，5 节点流程 |
| 9 | VEX Agent 判定逻辑 | 二 | 决策树 + 标准 VEX justification 标签 |
| 10 | 多 Agent 单元测试 20/20 | 二 | 全部通过 |
| 11 | Streamlit Dashboard | 三 | 4 页框架完成 |

### ⚠️ 待修复（已有代码但质量不达标）

| # | 问题 | 优先级 | 修复方案 | 排期 |
|:---|:---|:---|:---|:---|
| A1 | **Intel Agent 没有调用 IntelSkill** — 只读 engine_input 预加载数据，不能主动查 API | 🔴 高 | `intel_agent.py` 中集成 `IntelSkill.run()` 做实时查询 | **3.4 (周二)** |
| A2 | **Code Agent 没有调用 RemoteCodeSkill** — 只做静态模式表匹配 | 🔴 高 | `code_agent.py` 中集成 `RemoteCodeSkill.run()` 调用 GitHub API | **3.4 (周二)** |
| A3 | **Config Agent 没有调用 ConfigSkill** — 只从 engine_input 读已解析 SBOM | 🔴 高 | `config_agent.py` 中集成 `ConfigSkill.run()` 主动解析 SBOM 文件 | **3.5 (周三)** |
| A4 | **Baseline 实验不严谨** — 只有 4 个手工用例，Baseline 数据是模拟的 | 🟡 中 | 增加到 8-10 个 CVE 测试集，用 `nat eval` 跑真实 Baseline | **3.6 (周四)** |
| A5 | **prompts.yml 写了但没真正用** — VEX Agent 加载了但判定逻辑没引用 Few-shot | 🟡 中 | `_judge_vex_status` 中加入 prompt 模板参考逻辑 | **3.5 (周三)** |
| A6 | **GitHub Action 覆盖了原始 workflow** — `containerguard.yml` 把 NVIDIA 原文件覆盖了 | 🟡 中 | 恢复原文件，新 CI 改名为 `ci.yml` | **3.4 (周二)** |
| A7 | **Dashboard 用硬编码数据** — 没有读真实分析结果 | 🟢 低 | 加载 `baseline_results.json` + 支持上传 input_message 实时分析 | **3.7 (周五)** |

### 📋 待开发

| # | 任务 | 所属阶段 | 排期 |
|:---|:---|:---|:---|
| B1 | Baseline 对比报告文档 `docs/baseline_report.md` | 二 | 3.7 (周五) |
| B2 | PR 触发漏洞扫描 + 自动评论 | 三 | 3.10 - 3.14 |
| B3 | OPA 策略引擎 | 四 | 3.23 - 3.29 |

---

## 三、卢周全 — 全部未提交（0/9）

### 阶段一（本应 2.22 前完成，已严重逾期）

| # | 任务 | 截止 | 建议排期 |
|:---|:---|:---|:---|
| C1 | BRON 数据集下载与预处理 | ~~2.15~~ | **3.4 (周二)** |
| C2 | BRONLoader 数据加载器实现 | ~~2.19~~ | **3.5 (周三)** |
| C3 | KnowledgeGraph 图查询接口 | ~~2.22~~ | **3.6 (周四)** |

### 阶段二（截止 3.8）

| # | 任务 | 截止 | 建议排期 |
|:---|:---|:---|:---|
| C4 | BM25 稀疏检索模块 | 3.1 | **3.7 (周五)** |
| C5 | 图检索模块（Neo4j / NetworkX） | 3.4 | **3.8 (周六)** |
| C6 | 混合检索融合（BM25 + Graph） | 3.6 | **3.9 (周日)** |
| C7 | 报告生成模块 | 3.8 | **3.10 (周一)** |

### 阶段三-四

| # | 任务 | 截止 | 建议排期 |
|:---|:---|:---|:---|
| C8 | PR 报告输出模块 | 3.22 | 3.15 - 3.18 |
| C9 | 技术文档 / API 文档 | 4.5 | 3.28 - 4.2 |

> **⚡ 紧急：卢周全需要在 3.4 - 3.10 内集中补完阶段一 + 阶段二共 7 项任务**。建议组长每天催促进度。

---

## 四、邓一凡 — 已完成 3/6

### ✅ 已完成

| # | 任务 | 所属阶段 |
|:---|:---|:---|
| D1 | Log4j 测试输入 JSON + SBOM | 二 |
| D2 | Spring4Shell 测试输入 JSON + SBOM | 二 |
| D3 | Heartbleed 测试输入 JSON + SBOM | 二 |

### 📋 待完成

| # | 任务 | 截止 | 建议排期 |
|:---|:---|:---|:---|
| D4 | 测试用例映射表文档（CVE → 预期结果 → 实际结果） | 3.8 | **3.5 (周三)** |
| D5 | 端到端测试执行记录 | 3.22 | 3.12 - 3.16 |
| D6 | 最终测试报告 | 4.5 | 3.28 - 4.2 |

---

## 五、本周（3.3 - 3.8）紧急时间线

| 日期 | 金韩溢 | 卢周全 | 邓一凡 |
|:---|:---|:---|:---|
| **3.4 周二** | A1 Intel→Skill 打通 + A2 Code→Skill 打通 + A6 修复 CI | C1 BRON 数据集 | — |
| **3.5 周三** | A3 Config→Skill 打通 + A5 Prompt 真正集成 | C2 BRONLoader | D4 测试用例表 |
| **3.6 周四** | A4 扩充 Baseline 测试集 | C3 KnowledgeGraph | — |
| **3.7 周五** | A7 Dashboard 接真实数据 + B1 Baseline 报告 | C4 BM25 检索 | — |
| **3.8 周六** | 代码评审 + 阶段二收尾 | C5 图检索 | — |

---

## 六、风险提示

1. **卢周全进度为零** — 需立即沟通确认是否有困难，必要时组长协助或调整分工
2. **Agent ↔ Skill 断层** — 这是架构核心，答辩必问，优先级最高
3. **Baseline 数据的说服力** — 模拟数据在答辩时可能被质疑，尽量补跑真实对比
